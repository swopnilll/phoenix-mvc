services:
  db:
    image: postgres:18
    container_name: hello_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hello_dev
    ports:
      - "5433:5432"
    volumes:
      - hello_postgres_data:/var/lib/postgresql

volumes:
  hello_postgres_data:
